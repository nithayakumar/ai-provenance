requests>=2.31.0
Pillow>=10.0.0
beautifulsoup4>=4.12.0
watchdog>=3.0.0
lxml>=4.9.0

# Embedded metadata
python-xmp-toolkit>=2.0.1  # requires: brew install exempi (macOS) or apt install libexempi-dev
IPTCInfo3>=2.1.4

# C2PA manifest reading
c2pa-python>=0.5.0

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0

# Platform API keys (set as env vars — no extra packages needed)
# UNSPLASH_ACCESS_KEY, PEXELS_API_KEY
