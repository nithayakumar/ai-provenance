requests>=2.31.0
Pillow>=10.0.0
beautifulsoup4>=4.12.0
watchdog>=3.0.0
lxml>=4.9.0

# Embedded metadata — XMP requires: brew install exempi (macOS)
python-xmp-toolkit>=2.0.1
IPTCInfo3>=2.1.4

# C2PA manifest reading — gracefully skipped if not installed
# Install separately: pip install c2pa-python
# (prebuilt wheels available for macOS arm64 / x86_64 from PyPI)

# Testing
pytest>=7.4.0
pytest-mock>=3.11.0

# Platform API keys — no extra packages needed, set as env vars:
# export UNSPLASH_ACCESS_KEY=...
# export PEXELS_API_KEY=...
